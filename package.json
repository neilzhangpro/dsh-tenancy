{
  "name": "dsh-tenancy",
  "version": "0.3.0",
  "private": true,
  "description": "The missing tenant scope for DeepSeek Harness",
  "license": "MIT",
  "type": "module",
  "packageManager": "npm@11.6.4",
  "workspaces": ["packages/*", "examples/*"],
  "scripts": {
    "build": "tsc -b && npm run bundle --workspace dsh-plugin-tenancy && npm run build --workspace dsh-plugin-tenancy-suite",
    "check": "tsc -b --pretty false",
    "test": "npm run build && node --test packages/*/dist/*.test.js examples/tenant-dashboard/dist/*.test.js",
    "demo": "npm run build && node examples/two-tenants-one-process/dist/index.js && node examples/http-jwt-middleware/dist/index.js",
    "demo:dashboard": "npm run build && node examples/tenant-dashboard/dist/server.js",
    "verify": "npm run check && npm test && npm run demo"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.9",
    "typescript": "^5.8.3"
  }
}
