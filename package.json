{
  "name": "dsh-tenancy",
  "version": "0.1.0",
  "private": true,
  "description": "The missing tenant scope for DeepSeek Harness",
  "license": "MIT",
  "type": "module",
  "packageManager": "npm@11.6.4",
  "workspaces": ["packages/*", "examples/*"],
  "scripts": {
    "build": "tsc -b",
    "check": "tsc -b --pretty false",
    "test": "npm run build && node --test packages/core/dist/*.test.js packages/testing/dist/*.test.js",
    "demo": "npm run build && node examples/two-tenants-one-process/dist/index.js",
    "verify": "npm run check && npm test && npm run demo"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
